extends layout.pug

block content 
  article.grid-container.portfolio-intro
    div
      h1 Jeff Reeh's Portfolio
      p.lead.text-light
        | Built in Node.js and Express, I created this portfolio website to display what I have learned on my journey to a Full-Stack JavaScript Developer. 
  article.grid-container.portfolio-index
    
    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3

      each project in projects
        .cell
          a(href=`/projects/${project.id}`)
            img.thumbnail(src=`../static/img/${project.id}/tile.png`)
            h5= project.project_name